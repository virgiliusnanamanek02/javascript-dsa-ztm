function compessTwoBoxes(boxes1, boxes2) {
  boxes1.forEach(box => console.log(box))
  boxes2.forEach(box => console.log(box))
}

// The Big O Notation for this function is O(a + b) --> Because we have two different inputs
